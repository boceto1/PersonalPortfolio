import React from 'react';

import Button from './Button';

const ButtonStory = {
  component: Button,
  title: 'Components/Button',
};

export const DefaultButton = () => <Button color={'red'} />;

export default ButtonStory;
